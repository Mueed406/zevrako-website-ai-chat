{
  "name": "zevrako-website-chat-rules-tests",
  "private": true,
  "type": "module",
  "scripts": { "test": "firebase emulators:exec --only firestore 'vitest run'" },
  "devDependencies": { "@firebase/rules-unit-testing": "^5.0.0", "firebase": "^12.5.0", "firebase-tools": "^15.25.1", "vitest": "^4.0.8" }
}
